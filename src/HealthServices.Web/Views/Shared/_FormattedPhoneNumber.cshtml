@model string
@using System.Text.RegularExpressions

@{
    string value = string.IsNullOrWhiteSpace(Model)
        ? ""
        : Regex.Replace(Model, @"(\d{3})(\d{3})(\d{4})", "($1) $2-$3");
}

@value